[pytest]
# The plugin root is a Python package for Hermes, not for pytest — collection
# stays inside tests/ so the root __init__.py is never imported as a test module.
testpaths = tests
addopts = --import-mode=importlib
